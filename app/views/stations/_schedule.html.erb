<% [1, 2, 3, 4].each do |number| %>
  <span class="schedule">
    <% station.send(:"season#{number}").schedules.each do |schedule| %>
      <strong><%= schedule.day %></strong>: <%= schedule.start_time %> - <%= schedule.end_time %>
    <% end %>
  </span>
<% end %>
